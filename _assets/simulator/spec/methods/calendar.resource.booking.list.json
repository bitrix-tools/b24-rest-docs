{
  "$v": 1,
  "method": "calendar.resource.booking.list",
  "scope": "calendar",
  "kind": "read",
  "title": "Получить бронирования ресурсов по фильтру calendar.resource.booking.list",
  "permissions": "любой пользователь",
  "params": [
    {
      "name": "filter",
      "type": "object",
      "base": "object",
      "required": true,
      "description": "Поля фильтра",
      "ref": "#/defs/filterFields"
    }
  ],
  "defs": {
    "filterFields": {
      "dynamic": {
        "allowed": false,
        "ufAllowed": true,
        "note": "Пользовательские поля UF_* в схеме не описаны"
      },
      "fields": [
        {
          "name": "resourceTypeIdList",
          "type": "array",
          "base": "array",
          "required": true,
          "description": "Cписок идентификаторов ресурсов. Получить идентификаторы можно методом calendar.resource.list"
        },
        {
          "name": "from",
          "type": "date",
          "base": "date",
          "required": false,
          "description": "Дата начала периода"
        },
        {
          "name": "to",
          "type": "date",
          "base": "date",
          "required": false,
          "description": "Дата окончания периода"
        },
        {
          "name": "resourceIdList",
          "type": "array",
          "base": "array",
          "required": true,
          "description": "Список идентификаторов бронирований ресурсов из пользовательского поля типа resourcebooking у лидов или сделок в CRM. Получить идентификаторы можно: - универсальными методами — crm.item.get, crm.item.list - методами для лидов — crm.lead.get, crm.lead.list - методами для сделок — crm.deal.get, crm.deal.list Узнать какие пользовательские поля имеют тип resourcebooking можно методом crm.lead.userfiel…"
        }
      ]
    }
  },
  "requiredConvention": true,
  "confidence": "parsed",
  "source": {
    "file": "api-reference/calendar/resource/calendar-resource-booking-list.md"
  },
  "returns": [
    {
      "name": "result",
      "type": "array",
      "base": "array",
      "required": false,
      "description": "Массив объектов. Каждый объект описывает бронирование"
    }
  ],
  "page": "/api-reference/calendar/resource/calendar-resource-booking-list.html",
  "executable": false
}
