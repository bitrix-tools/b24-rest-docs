# Прокрутить родительское окно BX24.scrollParentWindow

Метод `BX24.scrollParentWindow` отправляет команду на прокрутку родительского окна до указанной вертикальной позиции. Начиная с версии модуля `rest` `25.800.0` метод можно использовать в [местах встраивания](../../../api-reference/widgets/index.md) приложения. Прокрутка отработает, если приложение открыто не в слайдере.

```js
void BX24.scrollParentWindow(Integer scroll[, Function callback])
```

## Параметры

{% include [Сноска об обязательных параметрах](../../../_includes/required.md) %}

#|
|| **Название**
`тип` | **Описание** ||
|| **scroll***
`integer` | Вертикальная позиция прокрутки родительского окна. Внутри метода значение приводится через `parseInt`, команда отправляется только если результат не `NaN` ||
|| **callback**
`function` | Функция обратного вызова, выполняется после отправки команды на прокрутку ||
|#

## Пример кода

{% include [Сноска о примерах](../../../_includes/examples.md) %}

```js
BX24.init(function () {
    BX24.scrollParentWindow(0, function () {
        console.log('Команда на прокрутку отправлена');
    });
});
```

## Обработка ответа

Метод не возвращает данные (`void`).

## Продолжите изучение

- [{#T}](./bx24-reload-window.md)
- [{#T}](./bx24-set-title.md)
